# 🎬 for-movies

Полноценный серверный проект на **Node.js + Express**, предназначенный для работы с данными о фильмах. Репозиторий включает серверную логику (контроллеры, маршруты, middleware) и фронтенд-часть в папке `public`.

---

## 📌 Возможности

* Обработка HTTP-запросов через Express
* Разделение логики на контроллеры и маршруты
* Поддержка middleware (например, аутентификация, логирование, валидация)
* Статические файлы в `/public`
* Готовая структура для расширения (подключение базы данных, JWT, REST API и т.д.)

---

## 🚀 Запуск проекта

### 1. Клонирование репозитория

```bash
git clone https://github.com/gunnishka/for-movies
cd for-movies
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Запуск сервера

```bash
node app.js
```

Или, если установлен nodemon:

```bash
nodemon app.js
```

После запуска сервер будет доступен по адресу:

```
http://localhost:3000
```

*(Порт может быть другим, если он указан в коде.)*

---

## 📁 Структура проекта

```
/controllers      # Логика обработки запросов
/middleware       # Промежуточные обработчики
/routes           # Маршруты Express
/public           # Статические файлы (HTML, CSS, JS)
app.js            # Главный серверный файл
package.json      # Описание проекта и зависимостей
```

---

## 🧠 Архитектура

Проект построен по классической MVC‑структуре:

* **Маршруты (`routes/`)** принимают HTTP‑запросы
* **Контроллеры (`controllers/`)** содержат бизнес‑логику
* **Middleware (`middleware/`)** добавляют промежуточные проверки
* **Public (`public/`)** хранит фронтенд ресурсы

Такой подход облегчает расширение и поддержку проекта.

---

## 🔧 Используемые технологии

* **Node.js** — серверная среда
* **Express.js** — HTTP‑сервер
* **JavaScript (ES6+)** — основной язык проекта

---

## 📌 Рекомендации по улучшению

* Добавить `.gitignore` и убрать `node_modules`
* Перенести конфигурацию (порты, ключи) в `.env`
* Добавить документацию API
* При необходимости подключить базу данных (MongoDB / PostgreSQL)

---

## 🤝 Вклад

PR и предложения приветствуются!

1. Форкните репозиторий
2. Создайте новую ветку
3. Внесите изменения
4. Откройте pull‑request

---

## 📄 Лицензия

Лицензия не указана — проект считается закрытым для коммерческого использования, пока автор не добавит LICENSE.md.

---

## 🖼 Скриншоты проекта

<img width="1462" height="761" alt="image" src="https://github.com/user-attachments/assets/bcfc2101-2238-4a55-9e8c-e0b7776fed40" />


---


<img width="1448" height="982" alt="image" src="https://github.com/user-attachments/assets/c69cf668-81a1-48c6-a913-2d9c833ebf4e" />

## 📬 Контакты

Если у вас есть вопросы или предложения — создайте Issue в репозитории.
